#!/bin/sh

set -e

#DEBHELPER#

for dsoname in suhosin ; do
    inifile=${dsoname}.ini

    dpkg-maintscript-helper mv_conffile /etc/php5/conf.d/${inifile} /etc/php5/mods-available/${inifile} 0.9.33-2~ -- "$@";
done

exit 0
